name: Deploy-to-beta
on:
  workflow_dispatch:
    inputs:
      build_mode:
        type: choice
        required: true
        description: 'Build-time NODE_ENV'
        default: 'prod'
        options:
          - prod
          - dev

jobs:
  deploy:
    uses: ./.github/workflows/deploy.shared.dev.yml
    with:
      NAIS_RESOURCE: .nais/dev-gcp/personbruker.yaml
    secrets: inherit
